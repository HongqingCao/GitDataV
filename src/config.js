//配置项
const host = 'http://localhost:5757'
//const host ='https://qr6yqgl4.qcloud.la'
const config = {
  host,
  loginUrl: `${host}/weapp/login`,
  userUrl: `${host}/weapp/user`
}
export default config
